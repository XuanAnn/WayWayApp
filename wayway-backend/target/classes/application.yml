server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: wayway-backend

wayway:
  momo:
    endpoint: ${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
    partnerCode: ${MOMO_PARTNER_CODE:MOMOBKUN20180529}
    accessKey: ${MOMO_ACCESS_KEY:klm05TvNBzhg7h7j}
    secretKey: ${MOMO_SECRET_KEY:at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa}
    redirectUrl: ${MOMO_REDIRECT_URL:http://10.0.0.2:8081/api/momo/return}
    ipnUrl: ${MOMO_IPN_URL:http://10.0.0.2:8081/api/momo/ipn}
    requestType: ${MOMO_REQUEST_TYPE:captureWallet}
    lang: ${MOMO_LANG:vi}
  ai:
    provider: ${AI_PROVIDER:OLLAMA}
    endpoint: ${OPENAI_RESPONSES_ENDPOINT:https://api.openai.com/v1/responses}
    apiKey: ${OPENAI_API_KEY:}
    model: ${OPENAI_MODEL:gpt-4.1-mini}
    ollamaEndpoint: ${OLLAMA_ENDPOINT:http://localhost:11434}
    ollamaModel: ${OLLAMA_MODEL:qwen2.5:3b}
